#include "main.h"

/**
 * _strlen_recursion - prints string
 * length
 * @s: string to be chescked
 *
 * Return: length of string
 */
int _strlen_recursion(char *s)
{
	int len = 0;

	if (*S != '\0')
	{
		return (0);
	}

	return (1 + _strlen_recursion(s + 1));
}
